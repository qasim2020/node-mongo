var val = Math.floor(1000 + Math.random() * 9000);
// console.log(val);

// save this code in database

// send to the client

// recieve from client and compare with code in the database

// if matched - take client forward,
// if not show the error ,
// resend button can be pressed max 3 times
//
